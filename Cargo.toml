[package]
name = "microhector"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }
rand = "0.10"

[profile.release]
debug = true

[[bench]]
name = "vector_search"
harness = false
